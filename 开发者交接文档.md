# 开发者交接文档

> 面向接手这个项目的人。记录「已经做了什么」「还没做什么」「怎么继续做」。
> 与 `README.md`（面向使用者）、`开发踩坑点.md`（问题记录）分工不同，本文档聚焦交接。

---

## 1. 项目现状

**一句话**：由营销落地页改造成的沉浸式番茄钟工作台，**P0–P3 全部完成并通过端到端验证**，
只剩主观听感、真机与部署等待确认项。

| 项 | 状态 | 证据 |
|---|---|---|
| 视觉层（四场景视频交叉淡入） | ✅ | 切场景后仅当前视频 `opacity:1` 且播放，其余暂停 |
| 番茄钟核心（状态机 / 配置 / 持久化） | ✅ | 受控实验跑通 `专注 → 短休 → 专注`，40 秒误差 0 秒 |
| 暂停 / 重置 / 跳过 | ✅ | 暂停后 1.6 秒读数不变；跳过不写入统计 |
| 音景（真实音源 / 场景绑定 / 音量） | ✅ | 场景切换音频交叉淡化，旧层 gain→0 后暂停 |
| 转场钟声 + duck + 开关 | ✅ | 阶段切换 masterGain 0.7 → 0.196；开关可关闭并持久化 |
| 设置与持久化 | ✅ | 改时长/音量/开关 → 刷新仍生效 |
| **任务清单（今日意图）** | ✅ | 增删改、设为进行中、勾选完成、徽标、刷新持久化全部验证 |
| **番茄自动计数** | ✅ | 完成任务从 `0/1` 变 `1/1`，写入日志 |
| **统计（今日 / 连续天数 / 7 天图）** | ✅ | 完成后显示「今日 1 个番茄，专注 1 分钟，连续 1 天」 |
| **系统通知** | ⚠️ 代码就绪 | 权限被拒时能优雅回退；**实际投递未验证**（自动化环境不弹权限框） |
| **呼吸引导（4-7-8）** | ✅ | 休息中检测到 `.breathe-478` 与文案「吸气 4 秒」 |
| **全屏专注模式（F）** | ⚠️ 部分 | 隐藏 UI 已生效；浏览器全屏在自动化环境被拒，**未在真机验证** |
| **移动端适配** | ✅ | 390×844 下抽屉满宽、无横向溢出、计时器可见 |
| **无障碍** | ✅ | 阶段切换 `aria-live`、4 个 stepper 带 `aria-live`、场景 `aria-pressed`、`:focus-visible` 描边 |
| **跨天归档 / 回顾** | ✅ | 模拟跨天：2 项未完成记为「未完成，已顺延」，已完成任务移出工作列表，提示「新的一天 · 2 项任务已顺延」 |
| **每日回顾界面** | ✅ | 任务面板「回顾」标签页按天展示任务快照 + 番茄数，并标注「N 项未完成，已顺延到次日」 |

---

## 2. 关键设计决策（不要轻易推翻）

| 决策 | 原因 |
|---|---|
| **计时用 `endAt` 时间戳**，不用累加自减 | 浏览器会节流后台标签页的 `setInterval`，自减必漂移。见 `开发踩坑点.md` 记录 7 |
| **音频用真实音源 + `HTMLAudioElement` 原生 `loop`** | 用户明确要求真实音源；长环境音（3–5 分钟）的 mp3 接缝不可闻 |
| **引擎实例放 ref 且不在卸载时 dispose** | StrictMode 会模拟卸载，销毁会导致开发环境音频彻底失效（记录 3） |
| **跳过（skip）不计入统计** | `onPhaseComplete(finished, next, credited)` 用 `credited` 区分自然完成与跳过 |
| **任务被设为「进行中」才吃番茄计数** | 避免番茄计数散落到所有任务上；勾选完成会自动取消进行中 |
| **日志上限 500 条** | 防止 localStorage 无限增长 |
| **统计数据实时计算，不做缓存** | 数据量小（≤500 条），`useMemo` 足够；避免缓存失效问题 |
| **无新增运行时依赖** | 图表用手写 SVG，通知/存储/音频全用原生 API |
| **归档用「增量写入」而不是「跨天快照」** | 用户可能当天就「清除已完成」甚至关掉应用，快照会丢掉记录；改成任务完成的当下就入档（见踩坑记录 13） |
| **跨天只顺延未完成任务，已完成任务移出工作列表** | 避免几天前就完成的任务在每天的回顾里重复出现 |
| **专注数据只有一份** | 归档只存任务快照；番茄数与时长一律来自 `lumora.log.v1`，避免两份数据不一致 |
| **全局空格只服务计时器，抽屉内保留原生行为** | 番茄钟里「空格 = 播放/暂停」是肌肉记忆；但抽屉里的开关也需要空格激活，用 `target.closest('aside')` 分流 |
| **抽屉关闭用 `visibility: hidden`** | 只做 `translate` 移出屏幕的话，键盘 Tab 仍会进入不可见的面板；`visibility` 参与过渡（规范保证过渡期间保持可见），动画不会被打断 |

---

## 3. 代码地图

| 文件 | 职责 | 改动注意 |
|---|---|---|
| `src/data/scenes.ts` | 四场景唯一数据源：视频、音景、文字色、遮罩 | **新增场景只需改这里 + 放音频文件** |
| `src/hooks/usePomodoro.ts` | 状态机 `idle → focus → shortBreak/longBreak → focus` | 时间戳驱动，别改成自减 |
| `src/hooks/useTasks.ts` | 今日意图 + 进行中任务 + 番茄计数 + 跨天归档 | 计数入口 `completePomodoro()`；跨天检测在文件末尾的 effect（挂载 / 30 秒轮询 / visibilitychange / focus） |
| `src/lib/review.ts` | 归档上限、日期标签、回顾聚合 | `buildReview` 是纯函数，改动后可直接用 Node 单测 |
| `src/hooks/useFocusLog.ts` | 专注日志 + 统计派生 | 日志写入入口 `addEntry()` |
| `src/audio/engine.ts` | 音频引擎（命令式，非 React） | React 侧只通过 `useAudioEngine` 调用 |
| `src/audio/chime.ts` | 合成转场钟声（不占文件体积） | 音色 = 正弦基频 + 2.756x 不谐和泛音 |
| `src/hooks/useAudioEngine.ts` | 引擎与 React 的桥 | 返回值必须保持 `useMemo` 稳定 |
| `src/lib/stats.ts` | 日期键、连续天数、7 天聚合 | streak 的口径：今天没记录时从昨天起算 |
| `src/lib/notify.ts` | 通知权限与投递 | 权限申请**只在用户开开关时**调用 |
| `src/lib/storage.ts` | localStorage 封装 + `usePersistentState` | 对象类型与默认值浅合并，新增设置字段无需迁移 |

### 调试用后门（仅开发环境）

`useAudioEngine` 在 `import.meta.env.DEV` 下把引擎挂到 `window.__lumoraAudio`，生产构建会被 tree-shake 掉：

```js
window.__lumoraAudio.isReady              // AudioContext 是否 running
window.__lumoraAudio.context.state        // 'running' | 'suspended'
window.__lumoraAudio.master.gain.value    // 主音量（含 muted / duck）
Array.from(window.__lumoraAudio.layers.entries())  // 当前场景的音频层
```

自动化断言锚点：`data-testid="timer"`、`data-testid="phase-status"`、
`footer button[aria-pressed="true"]`（当前场景）。
**不要用 aria-label 做稳定选择器**——它会随状态变化（踩坑记录 9）。

### localStorage 键位

| 键 | 内容 |
|---|---|
| `lumora.settings.v1` | 时长、音量、静音、自动开始、提示音、通知开关 |
| `lumora.scene.v1` | 当前场景下标 |
| `lumora.tasks.v1` | 今日意图数组 |
| `lumora.activeTask.v1` | 进行中任务 id |
| `lumora.log.v1` | 专注记录（≤500 条） |
| `lumora.archive.v1` | 按天归档的任务快照（≤180 天） |
| `lumora.lastActiveDay.v1` | 上次活跃日期，跨天检测的基准 |

> 归档口径：任务在被勾选完成的当下写入当天归档；跨天时把仍未完成的任务补录为「未完成，已顺延」，
> 并把已完成的从工作列表移除。因此「今日意图」每天只会剩下未完成的工作。

---

## 4. 剩余工作

原 P2/P3 清单已全部完成。以下为**新增的可选增强**，按价值排序：

1. **多场景/多设备一致性的响度归一化**：需要人耳听感判断，`scenes.ts` 的 `gain` 微调
2. **专注日志导出**（CSV / JSON）
4. **白噪音混音器**：每场景叠加附加声层，独立音量（引擎已支持多层，只缺 UI）
5. **番茄中断记录**：目前中断（暂停后重置）不留痕，可记录「中断次数」用于复盘
6. **PWA 化**：离线可用 + 桌面安装（视频与音频都是本地/CDN 资源，适合）
7. **部署上线**：可走 Cloud Studio，需在 `vite.config.ts` 确认 `base` 与音频路径

### 待用户确认

- [ ] 是否需要部署上线？
- [ ] 音景是否要换（备选素材 `waves / rain / window / storm` 已确认可下载，见踩坑记录 1）
- [ ] 「今日意图」是否需要每日自动清空/归档？

---

## 5. 如何扩展

### 新增一个场景

1. 把音频放进 `public/audio/`（建议 mp3，96kbps 以上，3–5 分钟）
2. 在 `src/data/scenes.ts` 追加一项（视频 URL、`layers`、`textColor`），并在 `SCENE_OVERLAY` 补一条
3. `SceneSwitcher` 自动多一个入口；快捷键 `1–9` 已支持（`App.tsx` 的正则 `/^Digit[1-9]$/`）
4. 更新 `README.md` §音源署名

### 替换 / 调整声音

| 想要的效果 | 改哪里 |
|---|---|
| 某场景整体更响/更轻 | `scenes.ts` 中该层的 `gain` |
| 环境音太吵、提示音听不见 | `engine.ts` 的 `DUCK_LEVEL`（默认 0.28） |
| 切换场景过渡太急 | `engine.ts` 的 `SCENE_FADE`（默认 1.2 秒） |
| 钟声太响/太轻 | `chime.ts` 中 `bus.gain` 与 `level` |
| 呼吸节奏不是 4-7-8 | `BreathingGuide.tsx` 的常量 + `index.css` 的 `breathe-478` 关键帧百分比（**两边必须同步改**） |

### 调整默认时长

改 `src/lib/defaults.ts`。注意：**老用户 localStorage 里的值优先**，
调默认值不会影响已有用户，需要的话自行加一版 storage key（`lumora.settings.v2`）。

---

## 6. 音源

来源与授权：见 `README.md` §音源署名（**CC0 公共领域，法律上无署名义务**，README 中已保留来源以示尊重）。

- 文件：`public/audio/{golden-hour,still-water,deep-woods,quiet-dawn}.mp3`，共 11.5MB
- 原始素材：Freesound CC0 录音，经 [`funcoder/omarchy-ambient`](https://github.com/funcoder/omarchy-ambient) 剪辑为循环
- 备选未用：`waves`（海浪）、`rain`、`window`（雨打窗）、`storm`

---

## 7. 验证清单

### 已通过的验证（自动化端到端，P0–P3 交付时）

- [x] `npm run build` 无类型错误
- [x] 闭环：专注 → 短休 → 回到专注，逐秒采样 40 秒误差 0 秒
- [x] 暂停后计时不走；跳过不计入统计（日志条数不变）
- [x] 刷新后设置 / 场景 / 任务 / 进行中任务全部保持
- [x] 四场景切换：画面与声音同步，旧层淡出后暂停
- [x] AudioContext 点击后进入 `running`，环境音实际播放
- [x] duck 生效（阶段切换瞬间 masterGain 0.7 → 0.196）
- [x] 任务：新增 / 设为进行中 / 勾选完成 / 徽标 / 面板底部计数
- [x] 番茄完成 → 进行中任务 `0/1` → `1/1`，日志写入 `{minutes, taskId, scene}`
- [x] 统计：「今日 1 个番茄，专注 1 分钟，连续 1 天」
- [x] 呼吸引导：休息中出现 `.breathe-478` 与「吸气 4 秒」
- [x] 专注模式：顶栏/底栏隐藏且不可点击，计时器仍可见；再按 F 恢复
- [x] 移动端 390×844：抽屉满宽、无横向溢出
- [x] 提示音开关可关闭并持久化
- [x] 空格全局切换计时；抽屉内空格激活开关且不影响计时
- [x] 抽屉关闭后 `visibility: hidden` 且 `aria-hidden="true"`，不参与 Tab 焦点
- [x] 完成任务在勾选当下即写入归档（所以「清除已完成」不会丢记录）
- [x] 跨天归档：未完成任务记为「未完成」并顺延、已完成任务移出工作列表、`lastActiveDay` 更新、弹出「新的一天 · 2 项任务已顺延」
- [x] 回顾标签页：按天展示任务快照与番茄数，往期未完成项标注「已顺延到次日」

### 尚未验证（必须人工确认）

- [ ] **主观听感**：4 个音景的响度与舒适度（自动化测不了，必须人耳听）
- [ ] **浏览器全屏**：自动化环境拒绝 `requestFullscreen()`，需在真机按 F 确认
- [ ] **系统通知实际投递**：需在真机允许权限后，切到别的窗口等一次阶段结束
- [ ] **后台挂 5 分钟回来看误差**：自动化环境会冻结标签页，未能复现真实场景
- [ ] iOS Safari（静音键会静音 Web Audio）
- [ ] 双屏 / 超宽屏视觉表现
