# 开发者交接文档

> 面向接手这个项目的人。记录「已经做了什么」「为什么这么做」「还没做什么」。
> 与 `README.md`（面向使用者）、`开发踩坑点.md`（问题记录）分工不同，本文档聚焦交接。

---

## 1. 项目现状

**一句话**：由营销落地页改造成的沉浸式番茄钟工作台，**P0–P4 全部完成并通过端到端验证**。

| 维度 | 状态 | 证据 |
|---|---|---|
| 番茄钟核心 | ✅ | 受控实验跑通 `专注 → 短休 → 专注`，40 秒误差 0 秒 |
| 会话持久化 | ✅ | 刷新后剩余时间 24:53 延续、状态仍为运行中 |
| 音景（真实音源） | ✅ | 场景切换交叉淡化，旧层 gain→0 后暂停 |
| **环境音跟随计时状态** | ✅ | 待机 `context === null`；按下开始才淡入；暂停后 4 层全部 `pause()`（记录 19） |
| **四层预热** | ✅ | 解锁后 `layerCount: 4`，全部 `readyState: 4`，切换瞬时 |
| 自适应垫层 | ✅ | 目标倍率 = 进度 × 强度（实测 0.054 → 0.114），增益跟随；起点无爆破 |
| 空间化 | ✅ | 各层 pan 分别为 -0.28 / +0.22 / -0.12，LFO 运行中 |
| 自动场景编排 | ✅ | 专注→Deep Woods、白天休息→Still Water、夜间休息→Quiet Dawn |
| 夜间模式 | ✅ | 遮罩 opacity 白天 0、夜间 1 |
| 任务 + 跨天归档 | ✅ | 未完成 2 项记为「未完成并顺延」，已完成移出工作列表 |
| 番茄计数 / 统计 / 日志 | ✅ | 任务 0/1 → 1/1，统计「今日 1 个番茄，1 分钟，连续 1 天」 |
| **分心自察** | ✅ | 采集 1 → 2 正确；跨越刷新仍为 1；最终写入日志 `interruptions: 1` |
| 本地洞察 | ✅ | 正确输出「9:00–11:00 高效时段」「平均 25 分钟」「Deep Woods 是主力场景」 |
| 准备倒计时 / 呼吸引导 | ✅ | 显示「3」、休息时出现 `.breathe-478` 与「吸气 4 秒」 |
| 焦点模式 | ⚠️ 部分 | 隐藏 UI 已验证；浏览器全屏在自动化环境被拒，需真机确认 |
| 系统通知 | ⚠️ 代码就绪 | 权限被拒时优雅回退已验证；**实际投递未验证** |
| 资源本地化 | ✅ | 7 个本地资源全部 200，外部字体链接数为 0 |
| PWA / 离线 | ✅ | SW 注册成功（scope `/`、缓存 `lumora-v1`）；**断网后仍能打开** |
| 性能 | ✅ | 运行中 5 秒内 5 次状态更新（开发模式计数翻倍后为 10） |
| 工程化 | ✅ | 80 个单测通过、ESLint 零警告、Prettier 就绪、git 4 次提交 |

---

## 2. 关键设计决策（不要轻易推翻）

### 计时与状态

| 决策 | 原因 |
|---|---|
| **计时用绝对时间戳 `endAt`** | 浏览器会节流后台标签页的 `setInterval`，自减必漂移。见 `开发踩坑点.md` 记录 7 |
| **转移规则抽成纯函数状态机**（`lib/pomodoroMachine.ts`） | 原先散落在多个 useCallback 里，加需求容易漏分支；现在有 23 个测试覆盖全转移矩阵 |
| **状态只在"显示的秒数"变化时更新** | 200ms 轮询只用于边界精度，不是渲染频率。改回高频更新会让整树每秒重渲染 5 次（记录 15） |
| **`finishPhase` 用 `credited` 区分完成与跳过** | 跳过不该计入统计、也不该推进长休 |
| **控制器成员在 App 里解构使用** | 让 `exhaustive-deps` 能看清每一项，依赖数组保持稳定，子组件的 memo 才不会被击穿 |

### 数据

| 决策 | 原因 |
|---|---|
| **归档用「增量写入」而不是「跨天快照」** | 用户可能当天就「清除已完成」甚至关掉应用，快照会丢记录（记录 13） |
| **跨天只顺延未完成任务** | 避免几天前完成的任务在每天回顾里重复出现 |
| **专注数据只有一份**（`lumora.log.v1`） | 归档只存任务快照，避免两份数据不一致 |
| **会话（含分心次数）持久化** | 刷新 = 丢掉 25 分钟，最伤信任；分心次数同理（记录 16） |
| **恢复会话时强制关闭 `autoStartNext`** | 否则"回来"时会白送一段已经流逝的时间 |
| **恢复窗口 6 小时** | 超过就视为放弃，不补记成绩 |
| **`usePersistentState` 与默认值浅合并** | 新增设置项不需要写迁移逻辑（已加 9 个设置项仍兼容老数据） |

### 音频

| 决策 | 原因 |
|---|---|
| **解锁时预热全部场景音层** | 切换场景是瞬时的；省流 / 2G 环境下退化为只预热当前场景 |
| **所有音量变化都走 `linearRampToValueAtTime`** | 直接改 `gain.value` 会爆音 |
| **duck 作用在 master 上** | 不干扰各层自己的目标音量 |
| **「解锁」与「发声」分离** | `unlock()` 只准备（音量保持 0），`setPlaying()` 才决定出声，由 `phaseStatus === 'running' \|\| ritual.isActive` 驱动。合在一起会导致"点一下场景就出声"（记录 19） |
| **睡眠淡出走独立的 `sleepGain` 节点** | 它和主音量是两套互不相干的比例；共用一个 gain 时两边会互相 `cancelScheduledValues`，表现为"开了睡眠定时反而全程没声音"（记录 19） |
| **自适应垫层用真实录音，不用合成振荡器** | 原先合成的那版 81% 能量压在 150Hz 以下、1kHz 以上为零 → 小喇叭放不出来就变成嗡鸣。现在是无音高的房间底噪（记录 20），只多 320KB |
| **垫层挂在每个场景的 `layers` 里** | 切场景时它同时属于新旧两边的期望音层，引擎不会把它当"要淡出的旧层"，音色连续不被切断 |
| **垫层默认关闭 + 可试听可调强度** | 它是氛围层，该由用户主动选；而且要能立刻听到才能判断强度（原先必须跑满一个番茄） |
| **空间化用 StereoPanner + 0.02Hz LFO** | 比 HRTF 便宜得多，耳机上的效果已经足够 |
| **引擎实例放 ref 且不在卸载时 dispose** | StrictMode 会模拟卸载，销毁会导致开发环境音频彻底失效（记录 3） |

### UI

| 决策 | 原因 |
|---|---|
| **视频等 `canplay` 再交叉淡化** | 否则切场景时先黑屏，新视频下载完才出现 |
| **每个场景一组渐变永远铺在视频下面** | 视频没就绪 / 加载失败 / 省电模式下页面依然成立 |
| **夜间压暗用独立遮罩层，不用 CSS `filter`** | `filter` 会让 `position: fixed` 的抽屉改为相对本元素定位 |
| **全局空格只服务计时器，抽屉内保留原生行为** | 番茄钟里空格=播放/暂停是肌肉记忆；抽屉里的开关也需要空格激活 |
| **抽屉关闭用 `visibility: hidden`** | 只做 `translate` 的话，键盘 Tab 仍会进入屏幕外的面板 |
| **抽屉常驻挂载 + `React.memo`** | 常驻是为了过渡动画；memo 是为了不让每秒的计时更新重算三百多行 JSX |

---

## 3. 代码地图

| 文件 | 职责 | 改动注意 |
|---|---|---|
| `src/config.ts` | 所有可调参数（音频时长、卡片节流、限额） | 调手感优先改这里 |
| `src/data/scenes.ts` | 场景唯一数据源 + 阶段→场景编排 + 渐变兜底 | 新增场景改这里 + 放音频 + 补 `SCENE_GRADIENT` |
| `src/lib/pomodoroMachine.ts` | 纯函数状态机 | 改转移规则必须同步 `pomodoroMachine.test.ts` |
| `src/lib/session.ts` | 会话序列化 / 恢复 | 恢复窗口与 `autoStartNext` 的处理见文件注释 |
| `src/lib/insights.ts` | 本地洞察 | 数据不足 6 条时不输出结论，别去掉这个门槛 |
| `src/lib/preset.ts` | 音景配方编解码 | 解码必须做白名单校验（场景 id、音量范围） |
| `src/hooks/useTasks.ts` | 今日意图 + 跨天归档 | 跨天检测在文件末尾的 effect（挂载 / 30 秒轮询 / visibilitychange / focus） |
| `src/audio/engine.ts` | 音频引擎 | 预热、空间化、duck、睡眠淡出都在这里；`unlock()` 与 `setPlaying()` 的职责边界见记录 19 |
| `src/data/scenes.ts` | 场景配置（含垫层） | `BED_LAYER` 被四个场景共用；垫层的响度由 `setAdaptive` 驱动，不在场景里 |
| `src/App.tsx` | 编排 + 布局 | 用 `latest` ref 给回调提供最新值，避免依赖膨胀 |

### 调试后门（仅开发环境，生产构建会被移除）

```js
window.__lumoraAudio.isReady              // AudioContext 是否 running
Array.from(window.__lumoraAudio.layers)   // 各层 { paused, gain, panner.pan }
window.__lumoraAudio.layers.get('/audio/focus-bed.mp3')  // 垫层 { adaptive, gain, paused }
window.__lumoraRenders                    // 渲染计数（验证性能约束没被破坏）
window.__lumoraAttention                  // 分心计数
```

> 注意：开发模式 StrictMode 会把每次渲染算两遍，`__lumoraRenders` 的增量要除以 2 才是真实更新次数。

自动化断言锚点：`data-testid="timer"` / `"phase-status"` / `"night-dim"`、
`footer button[aria-pressed="true"]`（当前场景）。
**不要用 aria-label 做稳定选择器** —— 它会随状态变化（记录 9）。

### localStorage 键位

| 键 | 内容 |
|---|---|
| `lumora.settings.v1` | 时长、音量、静音、自动开始、提示音、通知、仪式、自动场景、夜间、省电、自适应、空间化 |
| `lumora.scene.v1` | 当前场景下标 |
| `lumora.tasks.v1` | 今日意图数组 |
| `lumora.activeTask.v1` | 进行中任务 id |
| `lumora.log.v1` | 专注记录（≤500 条，含分心次数） |
| `lumora.archive.v1` | 按天归档的任务快照（≤180 天） |
| `lumora.lastActiveDay.v1` | 上次活跃日期，跨天检测基准 |
| `lumora.session.v1` | 运行中会话（阶段、剩余、完成计数、分心次数） |

---

## 4. 剩余工作

原清单已全部完成。以下是**有意保留的可选项**，按价值排序：

1. **多端同步**：目前 JSON 导出/导入已能手动迁移；再往上需要 WebDAV 或自建服务
2. **专注日志导出 CSV**（现在是完整备份 JSON）
3. **白噪音混音器 UI**：引擎已支持多层独立增益，只缺界面
4. **番茄中断记录**：现在只有"跳过"与"分心次数"，没有"中途放弃"的独立统计
5. **真机确认**：全屏、系统通知投递、iOS 音频（见验证清单）

### 明确不建议进主线的一项

**本地 LLM 总结回顾（WebLLM / transformers.js）**：需要下载 1–2GB 模型、
首屏加载与显存压力大、离线移动端体验差，而它能提供的价值（"你上午效率更高"）
已经被 `lib/insights.ts` 用纯数学在**零成本**下覆盖了 —— 后者的输出在实测中已经足够"像有个懂你的人"。
若确实要做，建议作为独立实验分支，并按需懒加载模型，不要进主 bundle。

### 待用户确认

- [ ] 是否需要部署上线（可走 Cloud Studio）？
- [ ] 音景是否要换（备选素材 `waves / rain / window / storm` 已确认可下载，见踩坑记录 1）
- [ ] 是否需要「今日意图」在跨天时自动清空（现在是顺延未完成项）

---

## 5. 如何扩展

### 新增一个场景

1. 音频放进 `public/audio/`（建议 mp3，96kbps 以上，3–5 分钟）
2. 在 `src/data/scenes.ts` 追加一项：`videoUrl`、`layers`（含 `pan` 和共用的 `BED_LAYER`）、`textColor`
3. 补 `SCENE_OVERLAY` 与 `SCENE_GRADIENT` 两条映射
4. `SceneSwitcher` 自动多一个入口；快捷键 `1–9` 已支持
5. 更新 `README.md` §音源署名

### 调整手感

| 想要的效果 | 改哪里 |
|---|---|
| 某场景更响 / 更轻 | `scenes.ts` 中该层的 `gain` |
| 环境音太吵、提示音听不见 | `config.ts` 的 `AUDIO.duckLevel` |
| 切换场景过渡太急 | `config.ts` 的 `AUDIO.sceneFadeSec` |
| 垫层太明显 / 太弱 | 设置里的「垫层强度」（默认 18%，约 -13dB），或 `lib/defaults.ts` 改默认值 |
| 垫层渐入太慢 / 太快 | `engine.setAdaptive` 的默认 `fadeSec = 6`（试听走 `AUDIO.playFadeInSec`） |
| 空间化幅度 | `config.ts` 的 `AUDIO.spatialDepth` / `spatialSweepHz` |
| 呼吸节奏不是 4-7-8 | `BreathingGuide.tsx` 常量 + `index.css` 的 `breathe-478` 关键帧（**两处必须同步**） |
| 准备倒计时时长 | `config.ts` 的 `UX.ritualSeconds` |

### 调整默认时长

改 `src/lib/defaults.ts`。**老用户 localStorage 里的值优先**，调默认值不影响已有用户；
确实要改所有人的值，需新增一版 storage key（`lumora.settings.v2`）并写迁移。

---

## 6. 音源与资源

- 环境音：`public/audio/*.mp3`（CC0，来源与录音者见 `README.md` §音源署名，共 11.5MB）
- 字体：`public/fonts/instrument-serif-latin-400-{normal,italic}.woff2`（43KB，原 Google Fonts）
- 浮层图：`public/overlay.webp`（195KB，原 1.9MB PNG @ figma.site）
- 背景视频：仍在 CloudFront（单场景几十 MB，不适合入库），**有渐变兜底**

换素材时记得同步更新 README 的署名表。

---

## 7. 验证清单

### 已通过的验证（自动化端到端）

**核心链路**
- [x] `专注 00:01 → 短休 01:00 自动开始 → 回到专注`，逐秒采样 40 秒误差 0 秒
- [x] 暂停后 1.6 秒读数不变；跳过不计入统计（日志条数不变）
- [x] 刷新后设置 / 场景 / 任务 / 进行中任务全部保持
- [x] **会话持久化**：运行中刷新 → 剩余时间 24:53 延续、状态仍为运行中

**音频**
- [x] AudioContext 点击后进入 `running`
- [x] **待机完全无音频活动**：`context === null`；待机切场景后依然 `null`
- [x] **开始才发声**：准备倒计时即 `isPlaying=true`、`master=0.7`、仅当前场景层播放
- [x] **暂停即静音且停止解码**：`master=0`、4 层全部 `pause()`、`currentTime` 冻结；继续后瞬时恢复
- [x] **睡眠定时**：`master` 保持 0.7、`sleepGain` 按时间缓降（1.9s 时 0.998）；被普通操作打断后回 1 并回到"跟随计时器"
- [x] **四层预热**：`layerCount: 4`，全部 `readyState: 4`，仅当前层播放
- [x] 空间化：各层 pan = -0.28 / 0 / +0.22 / -0.12，LFO 运行中
- [x] **自适应垫层**：目标倍率 0.054 → 0.114 随进度抬升，增益跟随（0.037 → 0.091）
- [x] **垫层起点无爆破**：开始时增益 0（修前实测会冒到 0.527，见记录 20）
- [x] **垫层默认关闭**，可手动打开；「试听」8 秒内 2 秒到达满强度（0.162）后自动收回并暂停
- [x] **垫层切场景不断**（倍率 0.010 → 0.010）；暂停后元素真正 `pause` 且增益 0
- [x] duck：阶段切换瞬间 masterGain 0.7 → 0.196

**数据与洞察**
- [x] 任务：新增 / 设进行中 / 勾选 / 徽标 / 面板底部计数
- [x] 番茄完成 → 任务 `0/1 → 1/1`，日志写入 `{minutes, taskId, scene, interruptions}`
- [x] 跨天：未完成 2 项记为「已顺延」、已完成移出工作列表、提示「新的一天 · 2 项任务已顺延」
- [x] 回顾页：按天卡片 + 「期间离开 N 次」+ 往期「已顺延到次日」
- [x] **本地洞察三条**：高效时段 / 平均时长 / 主力场景
- [x] **分心自察**：采集 1 → 2；跨越刷新仍为 1；最终落库 `interruptions: 1`
- [x] 音景分享链接：带 `#p=…` 打开 → 提示「已应用分享的音景配方」、场景与音量被还原、hash 被清理

**体验与工程**
- [x] 准备倒计时显示「3」，按钮切为「取消准备」
- [x] 呼吸引导：休息中出现 `.breathe-478` 与「吸气 4 秒」
- [x] 自动场景：专注→Deep Woods / 白天休息→Still Water / **夜间休息→Quiet Dawn**
- [x] 夜间模式遮罩：白天 0、夜间 1
- [x] 焦点模式：顶栏底栏隐藏且不可点击，计时器仍可见
- [x] 移动端 390×844：抽屉满宽、无横向溢出
- [x] 空格全局切换计时；抽屉内空格激活开关且不影响计时
- [x] 抽屉关闭后 `visibility: hidden`，不参与 Tab 焦点
- [x] **性能**：运行中 5 秒内 5 次状态更新（开发模式计数显示 10，StrictMode 双计）
- [x] **全流程零页面错误**：起停 / 双向 visibilitychange / 跳场景 / 重置 / 静音 / 6 个开关 / 睡眠定时 / 分享 / 任务与回顾 / 焦点模式
- [x] **生产构建**：开发探针全部为 `undefined`（已被 tree-shake）
- [x] **PWA 离线**：SW 注册成功、缓存 `lumora-v1`；断网后刷新**仍能打开**

### 尚未验证（必须人工确认）

- [ ] **主观听感**：4 个音景的响度与舒适度、空间化是否合适（自动化测不了，必须人耳听）
- [ ] **浏览器全屏**：自动化环境拒绝 `requestFullscreen()`，需真机按 F 确认
- [ ] **系统通知投递**：需真机允许权限后，切到别的窗口等一次阶段结束
- [ ] **Media Session**：耳机按键 / 锁屏控件的实际行为需要真机 + 蓝牙耳机
- [ ] iOS Safari（静音键会静音 Web Audio、PWA 图标需 Apple touch icon）
- [ ] 双屏 / 超宽屏视觉表现
- [ ] **离线时的音频**：SW 是首次请求才缓存，离线前未播放过的场景音频拿不到
