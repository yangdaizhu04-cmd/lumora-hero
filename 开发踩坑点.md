# 开发踩坑点

> 记录开发过程中真实遇到的问题、根因与解决方法。**只写已经实际发生的**，不写臆测。
> 格式：现象 → 根因 → 解决 → 备注。新条目追加到「记录」区末尾。

---

## 记录

### 1. 免费音源几乎全军覆没，只有 GitHub 可用

**现象**：按顺序尝试 Google Sound Library（`actions.google.com/sounds/v1/...`）、Wikimedia Commons、Mixkit、
Internet Archive，`curl` 全部返回 `HTTP 000`（连接失败/被墙）。切到 Pixabay：CDN 能通（`cdn.pixabay.com` 返回 200），
但搜索页是客户端渲染，HTML 里只有封面图，抓不到音频直链；开放 API 又需要 API key。

**根因**：本机网络对境外音频站点大面积不可达；Pixabay 的音频 URL 只在 JS 运行时注入。

**解决**：改用 GitHub 上已打包好的公共领域音源仓库
[`funcoder/omarchy-ambient`](https://github.com/funcoder/omarchy-ambient)：
其音效全部剪辑自 Freesound 的 **CC0** 录音，`raw.githubusercontent.com` 可直链，实测 200。

**备注**：`raw.githubusercontent.com` 与 `api.github.com` 在本机可达，可作为后续找素材的首选渠道。
候选备用音源：`waves.ogg`（海浪）、`rain.ogg`、`window.ogg`、`storm.ogg`，都还没用上。

### 2. ogg 转 mp3：体积与兼容性的权衡

**现象**：下载到的 4 个 `.ogg` 共 30MB，且 Safari 对 Ogg Vorbis 支持较晚（17+）。

**解决**：`ffmpeg -i x.ogg -codec:a libmp3lame -b:a 96k -ar 48000 -ac 2 x.mp3`，
体积降到 11.5MB（-62%），格式全平台通吃。

**备注**：MP3 编码器会在文件首尾写入 encoder delay/padding，理论上会破坏"无缝循环"。
本项目用 3–5 分钟的长环境音，接缝处几十毫秒的间隙在连续噪声中基本不可闻，实测未察觉。
若后续想彻底消除，方案见「遗留优化」。

### 3. StrictMode 下音频引擎被"销毁"，声音完全没了

**现象**：音频引擎实例放在 `useRef` 中，原计划在 `useEffect` cleanup 里 `dispose()`。
但 `main.tsx` 开着 `StrictMode`，开发模式下 React 会模拟一次卸载 → cleanup 执行 → `AudioContext` 被 close →
重新挂载时 ref 里的实例已经是"死了"的引擎，之后再也发不出声音。

**根因**：StrictMode 的 effect 双执行（mount → unmount → mount）与"单例资源必须显式释放"的写法冲突。

**解决**：
1. 引擎**不在卸载时销毁**（单页应用根组件不会真正卸载，注释已写明原因）；
2. `setScene()` 内部用「场景层签名」去重：签名相同则只同步音量，不重启播放，
   避免 StrictMode 双执行导致音频被 `play()` 两次或反复淡入。

**备注**：`stopAll()` / `dispose()` 仍保留，供将来真正需要清理时使用。

### 4. 阶段提示弹一次就再也不消失

**现象（写码时预判并规避）**：`PhaseToast` 原本把 `onDismiss` 放进 `useEffect` 依赖数组，
而父组件每 200ms 因倒计时重渲染一次 → 每次渲染都生成新的 `onDismiss` →
effect 反复重跑 → 清除并重建 2.6s 定时器 → 提示永远不消失。

**解决**：effect 只依赖 `toast.id`，并把 `onDismiss` 存进 ref 读取最新值；父组件的 `onDismiss` 也用 `useCallback` 固定。

**备注**：这是"高频重渲染父组件 + 子组件延时副作用"的经典陷阱，凡是子组件里有 `setTimeout` 都要检查依赖。

### 5. 稳定的回调引用：音频 API 对象不能每次渲染都重建

**现象（写码时预判并规避）**：`useAudioEngine()` 直接 `return { ... }` 字面量，
依赖它的 `useEffect` 会在每次渲染时重新执行 `setVolume/setMuted/setScene`。

**解决**：
1. hook 用 `useMemo` 返回稳定对象；
2. 引擎内部 `setVolume/setMuted` 做值比较短路（差值 < 0.001 直接 return），
   避免反复 `cancelScheduledValues` + `linearRampToValueAtTime` 造成 gain 自动化抖动。

### 6. 无用户手势时 `AudioContext` 处于 suspended

**现象（预判并规避）**：浏览器自动播放策略。

**解决**：`AmbienceEngine.unlock()` 只在用户手势（点击开始 / 点击场景 / 键盘切换）中调用，
内部 `ctx.resume()` 结束后立即重新应用当前场景层与主音量。
`unlockAudio()` 用 ref 做幂等，避免 StrictMode 下重复调用。

### 7. 后台标签页节流：让自动化测试出现"假 bug"

**现象**：自动化回归时，暂停瞬间读到的时间是 `04:54`，而按脚本推算应该是 `05:00`。

**排查过程**：发现焦点阶段的音频 `currentTime` 只有 4.25 秒，但两次测试帧之间实际隔了约 20 分钟
→ 说明页面在两次测试之间被浏览器**冻结/降频**了。

**根因**：标签页不可见时 Chrome 把 `setInterval` 节流到约 1 次/分钟，
所以 `finishPhase()` 没有按时触发，React 状态仍停留在 `running`，
而 `endAt` 已经过期 → 暂停时按"时间戳 - 当前时间"一算，直接扣掉 20 分钟。

**结论**：**这不是 bug，而是设计行为**——计时基于绝对时间戳，所以扣掉的时间是"真实流逝的时间"；
用户切回标签页时 `visibilitychange` 会立刻校正（这一点已在代码中实现）。
真实使用场景下用户就在看着页面，不会出现该现象。

**备注**：这也反向证明了时间戳方案的必要性——如果当初用自减计数，后台 20 分钟只会掉几分钟，计时会长期偏差。

### 8. 浏览器自动化验证环境的四个坑（Tabbit CLI）

| 现象 | 根因 | 解决 |
|---|---|---|
| 执行 135 秒的脚本返回 `EVALUATION_TIMEOUT` | 单帧有 **60 秒硬上限** | 把长流程拆成多帧，每帧 ≤ 50 秒 |
| 改了脚本重跑，返回的还是上一次的错误 | **复用 `--request-id` 会直接返回历史回执** | 修正后的代码必须用新的 request-id |
| 任务被中断后，页面变成 `about:blank`，`localStorage` 报 SecurityError | 任务终止后重建，标签页被保留但未导航 | 脚本开头判断 URL，不在目标页就先 `page.goto()` |
| 2.6 秒的 toast 抓不到 | 采样周期 600–1200ms，且判断条件用了更新后的时间文本，正好错过窗口 | 改用短轮询，或**换成可复现的触发动作**（用「跳过」按钮触发 toast，而不是等 60 秒自然完成） |

### 9. 自动化选择器被"随状态变化"的 aria-label 坑了

**现象**：验证任务清单时，用 `button[aria-label^="设为进行中"]` 只匹配到 1 行，但面板里明明是 2 条任务。

**根因**：任务被设为「进行中」后，同一个按钮的 `aria-label` 从 `设为进行中：X` 变成 `取消进行中：X`，
前缀匹配自然漏掉它。

**解决**：断言改用 `[aria-label*="进行中："]`；需要稳定锚点的场合（计时器）保留 `data-testid`。

**备注**：无障碍属性本来就应该随状态变化，别把它们当稳定选择器——要稳定就加 `data-testid`。

### 10. 自动化浏览器里 `requestFullscreen()` 不生效

**现象**：按 F 进入专注模式后，页面 chrome（顶栏/底栏）已正确隐藏，但 `document.fullscreenElement` 仍是 `null`。

**根因**：自动化浏览器的全屏权限与"用户激活"判定跟真人按键不同，请求被拒。

**解决**：代码对 `requestFullscreen()` 的 Promise 做了 `catch` 静默降级 ——
全屏被拒时，「隐藏 UI」的专注模式依然生效；同时监听 `fullscreenchange`，
用户按 Esc 退出全屏时会同步退出专注模式，状态不会错乱。

**备注**：真实浏览器下的全屏行为**未验证**，需要人工确认（见交接文档验证清单）。

### 11. 自动化环境不弹通知权限框

**现象**：点开设置里的「系统通知」开关，`Notification.permission` 仍为 `default`，开关自动回到关闭。

**根因**：自动化浏览器不弹权限 UI，`requestPermission()` 直接解析为 `default`。

**解决**：这是**正确的降级行为**——只有拿到 `granted` 才把设置置为开启，否则回退并弹提示「浏览器未授权通知」。
真实浏览器会弹权限框，用户允许后才写入设置。

**备注**：通知实际投递（尤其"页面在后台时才发通知"的分支）**未验证**，需要人工确认。

### 12. 浏览器进程活着，但 `newPage` 报 CDP 错误

**现象**：自动化脚本报
`browserContext.newPage: Protocol error (Target.createTarget): Task-scoped CDP command could not be dispatched`，
但 `diagnose` 显示一切正常（`ok: true`、`idle: true`、`quarantined: false`）。

**排查**：`tabs --task NAME` 返回 `{"tabs":[]}` —— 浏览器进程确实活着，但**一个窗口都没有**
（窗口被全部关掉、后台进程仍在运行）。

**解决**：重新执行一次浏览器可执行文件（在已有进程里开个新窗口），标签页创建立刻恢复正常。

**备注**：判断依据就是 `tabs` 返回空列表；`diagnose` **不会**报告"没有窗口"这种状态。

### 13. 归档功能的两个设计陷阱

**陷阱一：把「清除已完成」当作删除**。最初的方案是"跨天时对任务列表做快照"，
但用户完全可能在当天就点「清除已完成」，那些任务会从列表消失，跨天快照就再也拍不到它们 —— 回顾里会凭空丢掉一整天的成果。

**解决**：改成**增量归档** —— 任务被勾选完成的那一刻就写入「当天」归档，
跨天时只补录仍未完成的任务（标记为「未完成，已顺延」）。
这样无论用户什么时候清理列表、甚至当天就关掉应用，记录都不会丢。

**陷阱二：已完成的任务只在完成当天归档**。跨天顺延时如果无脑把"当前列表全部快照"，
几天前就完成的任务会在每一天的回顾里重复出现。

**解决**：跨天时只把 `done === false` 的任务记为未完成，已完成的任务从工作列表移除（它们已在完成当天入档）。

### 14. 三个外部域名是单点故障

**现象（预判 + 实测）**：视觉层依赖 3 个外部域名 —— Google Fonts（字体）、`soft-zoom-63098134.figma.site`（浮层 PNG，
**Figma 的临时站点域名，随时可能失效**）、CloudFront（4 个背景视频）。任一处挂掉，品牌视觉直接崩塌。

**解决**：
1. **字体**：抓到 woff2 镜像进 `public/fonts/`（共 43KB），`index.html` 删掉 Google Fonts 链接，
   改为 `@font-face` + `<link rel="preload">`。实测外部字体链接数为 0。
2. **浮层图**：下载后用 ffmpeg 转 WebP：**1,914,322 → 194,750 字节（-90%）**。
   注意用有损质量 88 即可 —— libwebp 的 alpha 通道默认是无损的，边缘不会发脏。
3. **视频**：单个几十 MB，不适合入库。改为**渐变兜底**：每个场景一组渐变永远铺在视频下面，
   视频没就绪或加载失败时页面依然成立（不会出现黑屏）。

**备注**：离线打开时控制台会有几条 `ERR_INTERNET_DISCONNECTED`，那是背景视频在跨域 CDN 上取不到 ——
预期行为，此时看到的是渐变而不是空白。

### 15. 200ms 状态更新 = 每秒 5 次整树重渲染

**现象**：计时器每 200ms `setState` 一次，而两个抽屉（`TaskPanel` 341 行、`SettingsDrawer` 249 行）
是**常驻挂载**的（只靠 `visibility` 隐藏），于是每秒有 5 次把整棵组件树重新求值。

**解决（四档，收益递减）**：
1. 状态机里判断"显示的秒数"是否变化，没变就**返回同一个 state 引用** → React 直接跳过重渲染
2. 进度环补间从 240ms 改成 950ms，配合 1 秒的更新依然平滑（25 分钟里 1 秒只占 0.07% 弧长）
3. 8 个组件加 `React.memo`
4. 把 `pomodoro.xxx` 解构出来使用，让 `exhaustive-deps` 能看清每一项 —— 依赖数组因此保持稳定，
   否则 memo 会被每次渲染新建的函数引用击穿

**实测**：运行中 5 秒内 10 次渲染（`window.__lumoraRenders`，仅开发环境）。
注意**开发模式 StrictMode 会把每次渲染算两遍**，所以实际是 5 次状态更新；优化前是 25 次更新。

### 16. 分心次数丢了：一次"数据看起来对、其实不对"的排查

**现象**：端到端验证时，完成 toast 明确显示「期间离开 1 次」，但写进日志的 `interruptions` 是 **0**。

**排查过程**：
1. 加开发期探针 `window.__lumoraAttention`，单独验证采集链路 → 计数正常（1 → 2 递增）
2. 用 24 秒的短专注跑完整链路 → 完成回调读到的**确实是 1**（toast 正确），但日志是空数组
3. 第 2 步顺带暴露一个边界：24 秒的专注 `Math.round(24000 / 60000) = 0` 分钟，
   被 `addEntry` 的"时长为 0 就丢弃"挡掉了（UI 最小 5 分钟，实际不会遇到）
4. 对比两次运行的差异 → 第二次运行前页面被浏览器**重载**过（`documentGeneration` 从 12 跳到 14）

**根因**：分心次数只存在内存里，页面一刷新就归零；结束回调本身完全正确。

**解决**：把 `interruptions` 一起写进会话存档，刷新后接着算
（`useAttention` 增加 `initialCount`，`usePomodoro` 增加 `syncInterruptions`）。
新增 3 个单测覆盖：往返、旧存档缺字段按 0、会话过期时一并清零。

**备注**：这个 bug 特别值得记 —— **toast 是对的、日志是错的**，如果只验证 toast 就会漏掉。
断言要落在最终持久化的数据上，而不是瞬时 UI。

### 17. 新功能把验证脚本自己绊倒了

**现象**：加了「会话持久化」之后，验证脚本点不到「开始专注」按钮，超时报错。

**根因**：上一段验证留下的会话在刷新后**被正确恢复了**，于是按钮的 aria-label 变成了「暂停」。

**解决**：验证脚本在重置状态时要连 `lumora.session.v1` 一起清掉。

**备注**：这类失败是"功能太成功"，不是 bug。但值得一提：**自动化脚本必须显式重置每一条持久化状态**，
否则会读到上一次运行的残留。

### 18. 开关旋钮冲出轨道：绝对定位的「静态位置」陷阱

**现象**：用户截图反馈设置里的开关错位 —— 白色旋钮跑到轨道右边缘**外面**去了。

**根因**：
```html
<button class="relative h-6 w-11 rounded-full">          <!-- 44 × 24 -->
  <span class="absolute top-0.5 ..." style="transform: translateX(22px)" />
</button>
```
`<span>` 只写了 `absolute`，`left` 是 `auto`。这种情况下浏览器采用元素的**静态位置**作为水平基准 ——
而 `<button>` 默认 `text-align: center`，静态位置是**居中**的（约 12px）。于是：

- 关闭态：12 + 2 = 14px 起步 → 旋钮占 14–34px（右移了 12px）
- 开启态：12 + 22 = 34px 起步 → 旋钮占 34–54px，**超出 44px 轨道 10px**

正好就是截图里看到的样子。

**修复**：显式写 `left-0.5`，开启态改成 `translateX(20px)`。
行程 = 44（轨道）− 20（旋钮）− 2×2（两侧留白）= 20px。

**验证方式（比肉眼可靠）**：用 `getBoundingClientRect()` 逐个量旋钮与轨道的关系，
断言「旋钮完全落在轨道内且四边留白一致」。实测 9 个开关在开 / 关两态下都是
上 2px、下 2px、左或右 2px，**零溢出**。

**教训**：
1. 绝对定位元素**必须显式给出定位基准**（`left` / `top`），不要依赖静态位置 ——
   父元素的 `text-align`、`padding`、行高都会影响它。
2. 布局 bug 用**几何断言**（读坐标算差）验证比截图肉眼看更可靠，也更容易沉淀成回归检查。
3. 这类问题我在构建、单测、lint 里都不可能发现 —— 只有真实渲染 + 人眼或坐标检查能抓到。

### 19. 「解锁」被当成了「播放」：用户还没开始就出声了

**需求**：环境音只在按下「开始专注 / 开始休息」时才有，待机、暂停都应该是安静的。

顺着这条需求改，一层一层暴露出三个问题。

**问题一：`unlock()` 里直接淡入主音量**

```ts
async unlock() {
  ...创建 ctx / 建好音层 / 配好场景...
  this.applyMasterGain(AUDIO.unlockFadeSec);   // ← 解锁即发声
}
```

而 `unlockAudio()` 被挂在"点场景切换器 / 按空格 / 媒体控制播放"等多个手势上 →
**点一下场景标签就会出声**，可用户此时只是在浏览场景。

**解决**：把"准备"和"发声"拆成两件事。

| 方法 | 语义 | 音量 |
|---|---|---|
| `unlock()` | 在用户手势里创建上下文、建好音层、配好场景 | 保持 0 |
| `setPlaying(bool)` | 由计时状态驱动：运行 / 准备倒计时 → true | 1.6s 淡入，暂停 0.8s 淡出 |

发声条件收敛成一行（`App.tsx`）：

```ts
const shouldPlayAmbience = phaseStatus === 'running' || ritual.isActive;
```

于是待机时**连 `AudioContext` 都不存在**（实测 `context === null`），待机切场景也保持安静。

**问题二：静音分支 `return` 得太早，媒体元素没被真正暂停**

改完上面这步，实测发现"暂停后主音量确实归 0，但音频元素还在播"：

```
⑤ 暂停后: { engine在播放: false, 主音量: 0, 正在播放的层: ["still-water.mp3"] }
```

**根因**：`applyLayerGains()` 里我把原来的 `if (this.sleeping) return;` 顺手改成了
`if (this.shouldSilence) return;` —— 这一 `return` 把紧随其后的"淡出后暂停媒体元素"分支
一起跳过了。听感上确实静音了（增益为 0），但元素仍在解码耗电，恰恰违背这次改动想省电的初衷。

**解决**：分清"目标增益是多少"和"要不要继续做后续动作" ——
目标增益用 `shouldSilence` 判断，提前 `return` 只留给睡眠（它有自己的收尾逻辑）。

**问题三：睡眠淡出和主音量抢同一个 `gain`**

实测睡眠定时：`{ engine在播放: true, 主音量: 0, 正在播放的层: ["deep-woods.mp3"] }` ——
层在播，主音量却是 0，等于全程静音。

**根因**：`handleStartSleep` 先 `setPlaying(true)`（排了一个 1.6 秒淡入斜坡），
紧接着 `startSleepFade()` 执行 `setValueAtTime(master.gain.value, now)` ——
此刻斜坡还没走，`gain.value` 读出来是 0，于是"15 分钟的淡出"变成
**从 0 爬到 0.0001**，永远听不见。两个自动化在同一时间线上互相 `cancelScheduledValues`。

**解决**：给睡眠淡出一个**独立节点**，两套比例彻底解耦：

```
layerGain → stereoPanner → master(音量/静音/duck) → sleepGain(睡眠淡出) → destination
```

取消睡眠时也不能读斜坡中的 `.value`（规范对"斜坡进行中读 value"的语义各实现不一），
改成记 `sleepStartedAt / sleepSeconds`，按时间推算当前比例再往回拉：

```ts
const current = wasFading
  ? clamp(1 - (now - this.sleepStartedAt) / this.sleepSeconds, 0.0001, 1)
  : gain.value;   // 没在淡出时 .value 才是可靠的
```

**验证结果（均为实测数值）**

| 场景 | 观测 |
|---|---|
| 待机 | `context === null`、音层为空 —— 完全没有音频工作 |
| 待机切场景 | 依然 `context === null` |
| 准备倒计时 | `isPlaying=true`、`master=0.7`、仅当前场景层播放 |
| 运行中 | 其余 3 层"静默且已暂停"，无"有增益却暂停"的层 |
| 暂停 1.8s 后 | `master=0`、4 层全部 `pause`、`currentTime` 冻结 |
| 继续 | 主音量回到 0.7、当前层重新播放（无需重新下载） |
| 睡眠定时 | `master` 保持 0.7、`sleepGain=0.998`（1.9s / 900s，正好对上） |
| 睡眠中被打断 | `sleepGain` 回 1、`isPlaying=false`、全部暂停 |

**教训**：
1. **一个 `AudioParam` 只让一处逻辑负责**。多个功能共用一个 gain 时，任何一方的
   `cancelScheduledValues` 都会静默打断另一方 —— 这类 bug 不报错，只表现为"没声音"。
2. **拆分"准备"与"生效"**：`unlock` 只解决浏览器的自动播放限制；"什么时候该出声"
   应该由状态单独驱动，两件事混在一个方法里，必然出现越权发声。
3. **静音 ≠ 停止**：增益归零管听感，媒体元素暂停管耗电，两件事都要做。

### 20. 合成的低频长音「很恼火」：从频谱证据到换成真实录音

**反馈**：自适应音景随进度铺入的那层低频长音，听了让人很恼火。

**第一步：先拿客观证据，别猜**

把当时的合成 pad（C2 + G2 + C3 正弦叠加）用 ffmpeg 复现出来，量了两组指标：

| 素材 | <150Hz 能量占比 | >1kHz 能量占比 | 频谱形态 |
|---|---|---|---|
| 原合成 pad | **81.3%** | **0.0%** | 三条静止的亮线 |
| brown noise | 87.1% | 2.0% | 弥散的云 |
| room tone | 53.7% | 8.5% | 弥散的云 |
| G#maj7 暖 pad | 13.5% | 3.0% | 一排等间距谱线 |

结论（三条互相印证）：

1. **81% 的能量压在 150Hz 以下、1kHz 以上一点都没有** —— 笔记本和手机扬声器在这个频段
   基本没有输出能力，放不出来的低频会变成**互调失真**，听感就是嗡鸣。
2. **静态的离散谱线 = 有明确音高**。真实录音（鸟鸣、水流、风）不携带音高，大脑可以忽略；
   而持续音会被当作"旋律"持续占用注意力 —— 这是长时间听会烦躁的关键。
3. **窄带纯音的感知响度被数值低估**：`padMaxLevel = 0.16`（-16dB）看起来很低，
   但环境音是宽带噪声（能量摊在整个频谱），pad 是窄带纯音（能量集中在 3 个频点），
   同电平下纯音听起来突出得多。

**第二步：换成真实录音**

用 freesound 的 CC0 素材（搜索页可以直接解析出条目，预览 mp3 无需登录即可下载）。
定稿是 richwise 的 `RoomTone07`：**无音高、无突发杂响**（峰值只比均值高 13dB，
有椅子响/车声这类事件会高出 30dB 以上），且 1kHz 以上有 8.5% 的"空气感"。

处理成 27.2 秒无缝循环（3 秒 wrap 交叉淡化）+ 峰值归一化到 -3dB + 单声道 96kbps = **320KB**。

**踩到的三个坑**

**坑一：mp3 的时长元数据不可信。**
`ffprobe -show_format` 对这套 VBR 且缺 Xing 头的预览文件读出来是错的
（`brown-noise` 报 3600 秒、`roomtone-07` 报 599.7 秒，而实际分别是 49.4 / 30.2 秒）。
必须按解码帧数算：`nb_read_frames × 1152 / sample_rate`。
差点被这个假数据带去做一个 15 分钟的循环。

**坑二：新建音层的默认倍率是 1，垫层在专注起点"冒"了一段声音。**

实测抓到：

```
起点: { 剩余59s, 进度0.02, 目标倍率: 0.003, 实际增益: 0.527 }   ← 满强度的 58%
```

根因是时序：音层是**解锁时**才创建的，App 的 `setAdaptive(bed, 0)` 在音层还不存在时
就调用了，`this.layers.get(src)` 返回 undefined 直接 return —— 这一次调用**丢了**。
随后解锁流程创建音层，`ensureLayer` 里 `adaptive: 1`（对场景层是对的）→ 目标变成
`0.9 × 1 = 0.9`，于是垫层按 1.2 秒的斜坡冲到 0.53 才被下一次 effect 拉回来。

修法：引擎记住每个音源的**目标倍率**，`ensureLayer` 用它作为初始值。

```ts
private adaptiveTargets = new Map<string, number>();

setAdaptive(src, factor, fadeSec = 6) {
  this.adaptiveTargets.set(src, next);      // 音层还没建也要记下
  const layer = this.layers.get(src);
  if (!layer) return;
  ...
}

// ensureLayer 里
adaptive: this.adaptiveTargets.get(src) ?? 1,
```

这类 bug 的特征是**一次性的、只在特定的首次时序下出现**，肉眼很难复现；
能抓到它纯粹是因为验证脚本把"起点"也当成一个采样点。

**坑三：试听只有 8 秒，而淡入要 6 秒。**
`setAdaptive` 内部固定用 6 秒斜坡（随进度漂移本来就该慢），结果试听刚起来就结束了。
改成可传 `fadeSec`，试听走 `AUDIO.playFadeInSec`（1.6 秒）—— 实测 2 秒时已到满强度。

**验证结果（21 条断言全过）**

| 场景 | 观测 |
|---|---|
| 垫层默认状态 | 设置里 `adaptiveSound` 默认 false、强度显示 18% |
| 试听 | 引擎开始播放、倍率 0.18、2 秒到满强度 0.162、8 秒后收回并暂停 |
| 起点 | 目标 0、增益 0、元素未播放（无爆破） |
| 随进度 | 倍率 0.054 → 0.114，增益 0.037 → 0.091 |
| 切场景 | 倍率 0.010 → 0.010，不受影响 |
| 暂停 | 元素 `pause`、增益 0、环境音一并静音 |

### 21. 中央提示的层级低于抽屉遮罩：删除任务后「撤销」点不到

**现象**：浏览器验证"删除任务 → 撤销"这条链路时，脚本报
`div.fixed.inset-0.z-[80] intercepts pointer events` —— 抽屉的黑色遮罩（`z-[80]`）
盖在中央提示（`z-[70]`）上，提示里的「撤销」按钮看得见、点不到。

**根因**：两层都是 fixed 全屏，遮罩层级更高。此前提示只有文字（容器 `pointer-events-none`），
"点不到"这件事不存在；给它加上按钮之后才第一次需要"点得到"，层级问题随之暴露。

**解决**：提示层提到 `z-[100]`（高于抽屉 `z-[90]` 与遮罩 `z-[80]`），容器继续 `pointer-events-none`，
只有按钮自己 `pointer-events-auto` —— 既点得到，也不会挡住抽屉里的其他操作。

**教训**：给"只读"组件加交互元素时，要连同它的层级与命中区域一起重新审一遍。
`pointer-events-none` 的组件里塞一个按钮，是最容易漏的组合；这类问题单测与 lint 都看不见。

### 22. 懒挂载的数组没去重，渲染出两个同 key 的 video

**现象（浏览器验证发现）**：把"4 个 `<video>` 全挂载"改成"按需挂载"后，在页面里数
`document.querySelectorAll('video')`，数量比预期多；比对 `currentSrc` 才确认同一个场景被渲染了两次。

**根因**：保留逻辑写成 `[activeIndex, displayedIndex, ...prev]`，而这两个值**绝大多数时候相等**
（新场景就绪后 `displayedIndex` 会追上 `activeIndex`），于是产生重复项。
重复项会渲染出两个 `key` 相同的 `<video>`：React 报 key 冲突，还白跑一路解码与内存。

**解决**：`Array.from(new Set([activeIndex, displayedIndex, ...prev]))` 先去重，再截断到上限。

**教训**：凡是"多个来源 + 历史保留"的集合合并，顺序都应该是 **去重 → 截断**。
这条只靠单测（纯函数层）是发现不了的，必须在浏览器里数元素。

### 23. 写盘去抖与"清空数据"打架：刚清掉的数据被 flush 写回

**现象（改去抖时预判，随后被验证脚本撞上）**：验证脚本 `localStorage.clear()` + reload 之后，
页面读到的仍是清空前的值 —— 说明 clear 之后又有一次写入把数据装了回去。

**根因**：`usePersistentState` 的去抖定时器还挂着（`dirty = true`），`pagehide` / 卸载时的 flush
会把内存里的旧值写回 localStorage。这个 flush 本来是为"刚拖完音量就关页面"设计的，
但它同样会在"清空数据"之后把数据救回来。

**解决**：`storage.ts` 暴露 `suspendPersistence()`，`clearAllData()` 第一步就挂起写入，
之后所有 flush 变成空操作；ErrorBoundary 的"重置数据并重启"走同一条路径。

**教训**："延迟写入"与"清空 / 重置"天生互斥。任何去抖写入都必须提供显式的取消 / 挂起开关，
否则用户点了"清除全部数据"，数据会在下一次页面隐藏时自己回来。

### 24. 本地全绿、CI 全红：jsdom 在 Node 20 上初始化即崩

**现象**：`npm test` 本地 13 文件 / 119 用例全绿；推到 GitHub 后 Actions 29 秒就挂了，日志显示

```
Test Files  9 passed (13)
     Tests  97 passed (97)
Vitest caught 4 unhandled errors during the test run.
```

**4 个测试文件一个用例都没跑起来**（备份 + 3 个 hooks），且报错不是断言失败，而是环境初始化就炸：

```
TypeError: webidl.util.markAsUncloneable is not a function
  ❯ new CacheStorage node_modules/undici/lib/web/cache/cachestorage.js:20:17
  ❯ Object.<anonymous> node_modules/undici/index.js:179:25
  ❯ Object.<anonymous> node_modules/jsdom/lib/api.js:12:33
```

**根因**：CI 写的是 `node-version: 20`，而依赖早已把底线抬高：

| 包 | `engines.node` |
|---|---|
| `jsdom@30.1.0` | `^22.22.2 \|\| ^24.15.0 \|\| >=26.0.0` |
| `undici@8.10.2`（jsdom 的依赖） | `>=22.19.0` |

`markAsUncloneable` 来自 `node:worker_threads`，**Node 20 根本没有这个导出**，
于是 jsdom 只要被加载就抛错 —— 而 `// @vitest-environment jsdom` 的 4 个文件首行就要求加载它。

本地是 Node 24.16.0，正好在 `^24.15.0` 区间内，所以本地"看不到"这个问题。

**解决**：`ci.yml` 的 `node-version` 由 20 改为 **24**（与本地开发版本对齐），CI 26 秒转绿。

**教训**：

1. **CI 的运行时版本必须与实际开发版本对齐**，否则依赖升级在本地永远"看起来没问题"。
   npm 对 `engines` 不满足默认只给 warning、照样装，所以这种问题只会在真正运行时炸。
2. 排查这类"环境才有的错"要**先读 `engines` 字段**，比逐行读堆栈快得多：
   `node -e "console.log(require('./node_modules/jsdom/package.json').engines)"`。
3. 想更早发现：`package.json` 补 `engines` + `.npmrc` 里 `engine-strict=true`，
   版本不符会在 `npm ci` 阶段就失败，而不是等到跑测试。
4. 一个反直觉的细节：报错信息里的 `CacheStorage` 跟本项目毫无关系 ——
   它是 jsdom 加载时的连带产物，别被它带偏去查缓存逻辑。

### 25. `git push` 连不上 github.com，但 `gh api` 一切正常

**现象**：向新建的仓库推送时连续 3 次失败，每次都在 21 秒后超时：

```
fatal: unable to access 'https://github.com/<user>/<repo>.git/':
Failed to connect to github.com port 443 after 21042 ms: Could not connect to server
```

**但同一时刻** `gh api user` 正常返回账号资料，`api.github.com` 完全可用。

**排查**（每一步都要有结论，别停在"网络问题"）：

| 检查 | 结果 | 说明 |
|---|---|---|
| `Resolve-DnsName github.com` | `20.205.243.166`，无 AAAA | DNS 正常，不是解析污染 |
| `Get-Command gh` | 已安装且已登录 | 凭据没问题 |
| `GIT_CURL_VERBOSE=1 git ls-remote` | `Trying 20.205.243.166:443... connect failed: Timed out` | SYN 被丢包，连接层就没建立 |
| Node `fetch('https://github.com/')` | `UND_ERR_CONNECT_TIMEOUT` | 换一个 HTTP 栈复现，排除是 git 自己的问题 |

结论：**`api.github.com` 可达，`github.com:443` 被阻断**。所以"API 能用"不等于"`git push` 能用"。

**解决**：改走官方提供的 SSH-over-443 通道（`ssh.github.com:443`）。本机 `~/.ssh/id_ed25519`
已注册在该账号上，先验证通道：

```bash
ssh -T -p 443 git@ssh.github.com
# Hi <user>! You've successfully authenticated, but GitHub does not provide shell access.
```

推送时**不改 origin**，用一次性远端地址：

```bash
git push ssh://git@ssh.github.com:443/<user>/<repo>.git master:master
```

**备注**：

1. 注意 `known_hosts` 里的记录是 `[ssh.github.com]:443`（带方括号的端口形式），首次连接要确认指纹。
2. 用一次性地址推完后，本地 `refs/remotes/origin/master` 不会自动更新，`git status` 会显示
   `ahead 1`（其实已经同步）。要么等网络恢复 `git fetch`，要么用
   `git update-ref refs/remotes/origin/master $(git rev-parse HEAD)` 手工对齐。
3. 若 `github.com` 长期不通，再把它固化：`git remote set-url origin ssh://git@ssh.github.com:443/<user>/<repo>.git`。
4. `gh` 的 API 通道全程可用 —— 查仓库、看 CI 日志（`gh run view --log-failed`）都不受影响，
   只有 `git` 的 443 传输会卡。排查时先把这两条链路分开看。

---

### 26. 下载 GitHub 仓库里的文件：`raw.githubusercontent.com` 也不通

**现象**：给混音器补 CC0 音源时，Node `fetch('https://raw.githubusercontent.com/funcoder/omarchy-ambient/main/sounds/rain.ogg')`
直接 `fetch failed` / `terminated`；同一时刻 `api.github.com` 一切正常。

**判断**：和记录 25 是同一个根因 —— `github.com:443` 被阻断，`raw.githubusercontent.com`
属于同一条传输链路。不要因为"API 能用"就默认"直链也能用"。

**解决**：改走 GitHub API 的 blob 通道，两步：

```js
// 1. 拿文件的 blob sha（支持 recursive，一次列出全仓库）
const tree = await (await fetch(
  `https://api.github.com/repos/${repo}/git/trees/main?recursive=1`,
)).json();

// 2. 按 sha 取内容（base64）
const blob = await (await fetch(
  `https://api.github.com/repos/${repo}/git/blobs/${sha}`,
)).json();
fs.writeFileSync(name, Buffer.from(blob.content, 'base64'));
```

实测 3–12MB 的文件约 2 秒一个，六个音源十几秒下完。

**备注**：不要用 `/repos/{o}/{r}/contents/{path}` —— 它对大于 1MB 的文件只返回元数据、
不给 `content`，大文件必须走 `git/blobs`。

---

### 27. 循环接缝：线性交叉淡化会在交叉区掉 3dB

**现象**：把 3–5 分钟的原始录音裁成 47 秒循环，用 wrap 交叉淡化消除首尾接缝。
第一版直接用 `afade` 的默认曲线，接缝不爆音了，但循环时每 47 秒能听出一次音量起伏。

**分析**：`afade` 默认是**线性**的。交叉区两个信号的增益分别是 `t` 和 `1-t`，
对不相关噪声而言能量和是 `t² + (1-t)²`，中点只有 0.5 —— 也就是 **-3dB 的凹陷**。
接缝越频繁，这个凹陷越容易被听到。

**解决**：换成等功率曲线：

```bash
afade=t=in:d=3:curve=qsin     # sin(π/2 · t)
afade=t=out:d=3:curve=qsin    # sin(π/2 · (1-t)) = cos(π/2 · t)
```

两者平方和恒为 1（`sin² + cos²`），交叉区能量恒定，凹陷消失。

**验证**：不靠耳朵。解码成 PCM，按 20ms 分帧算 RMS，比较"首尾 5 帧均值之差"
与"全曲相邻帧 RMS 跳变的平均值"：

```
rain       head=0.0625  tail=0.0712  seamJump=0.00870  avgJump=0.00557   OK
crickets   head=0.0513  tail=0.0528  seamJump=0.00154  avgJump=0.00130   OK
```

判据 `seamJump < 3 × avgJump`：接缝处的跳变不超过素材自身的正常起伏，即认为连续。

**备注**：47 秒素材还会带来"听出它在循环"的问题（那是素材长度决定的，不是接缝）。
真要解决得在引擎里做双源交叉淡化（见「遗留优化」1）。

---

### 28. 新增设置项漏改备份导入的手写白名单

**现象**：给 `PomodoroSettings` 加了 `mixerEnabled` / `mixerLevels` 之后，`tsc -b` 报错：

```
src/lib/backup.ts(59,3): error TS2739: Type '{ ... }' is missing
the following properties from type 'PomodoroSettings': mixerEnabled, mixerLevels
```

**原因**：`sanitizeSettings()` 是**逐字段手写**的白名单，不会自动跟随类型变化。
加类型字段时很容易只想着 `defaults.ts`，忘了这里。

**更危险的是嵌套对象**：`mixerLevels` 是 `Record<MixerSourceId, number>`。
即便补上了字段，老备份里根本没有这个键，而 `usePersistentState` 的浅合并
（规避清单 7）**只覆盖第一层** —— 缺的不是整个对象，是对象里的某个 id，
于是滑块读到 `undefined` 直接崩，且不会有任何编译错误。

**解决**：三个写入口（默认值 / 预设 / 备份导入）统一过 `normalizeLevels()`：
缺键补 0、越界夹取、非数字丢弃；读取侧再按 id 逐个兜底（`?? 0`）。

**教训**：新增设置项的 checklist 是三处，不是一处 ——
`types.ts` → `defaults.ts` → `backup.ts 的 sanitizeSettings`，再加一条单测。

---

### 29. 拖拽排序的落点换算差一位

**现象**：手写的拖拽排序，把任务往下拖一格时看起来"没反应" ——
插入指示线画在了正确的位置，松手后顺序却没变。

**原因**：把"指针指向的插入位置"直接当成了"移动后的最终索引"。
但往下拖时被拖的那一项会**先被抽走**，它后面的位置整体前移一格，
所以插入位置总比最终索引大 1：

```
[A, B, C]  把 A 拖到 C 的下方 → 插入位置 = 3（末尾）
抽走 A 后变成 [B, C] → 正确落点是索引 2，而不是 3
```

**解决**：抽成纯函数，而不是在事件回调里内联一个三元表达式：

```ts
export function resolveMoveTarget(fromIndex: number, insertAt: number): number {
  return insertAt > fromIndex ? insertAt - 1 : insertAt;
}
```

三种情况各有单测：往上拖（不减）、往下拖（减 1）、拖回原位（结果等于 `fromIndex`，
调用方据此跳过写入）。

**为什么值得单独抽**：这一步在 jsdom 里**测不到** —— `getBoundingClientRect()`
返回的矩形恒为全 0，落点计算拿不到真实几何。把纯计算从 DOM 里剥出来，
是让这条最易错的逻辑唯一能被覆盖的方式（真实拖拽另由浏览器实测覆盖）。

---

### 30. 触屏上按住拖拽手柄，浏览器会去滚列表

**现象**：桌面鼠标拖拽正常；触屏上按住手柄上下移动时，页面开始滚动，手柄纹丝不动。

**原因**：浏览器默认把"单指按住并移动"当成滚动手势 —— 在它看来这和拖拽没有区别，
我们的 `pointermove` 要么收不到，要么收到一两次就被 `pointercancel` 掐断。

**解决**：手柄上加 `touch-action: none`，明确声明这块区域的触摸序列由脚本处理：

```tsx
<button style={{ touchAction: 'none' }} onPointerDown={...}>
```

**备注**：只加在手柄上，不要加在整行 —— 整行禁掉的话，列表就没法用手指滚动了。

**验证状态**：桌面路径（鼠标指针事件）已用真实浏览器实测通过（下拖 / 上拖 / 刷新后保持）；
触屏路径需要在真机上确认，桌面 Chrome 的设备模拟不能作为依据。

---

## 规避清单（编码前已知的风险，已提前处理）

| # | 风险 | 处理方式 | 状态 |
|---|---|---|---|
| 1 | `AudioContext` 无手势时 suspended | 首次点击/键盘操作时才 `resume()` | ✅ 已验证（`ctxState: running`） |
| 2 | 后台标签页定时器节流导致计时漂移 | 计时基于绝对时间戳 `endAt` + `visibilitychange` 立即校正 | ✅ 已验证（见记录 7） |
| 3 | 4 个 `<video>` 同时解码浪费资源 | 只播放当前场景，其余 `pause()`，非当前场景 `preload="metadata"` | ✅ 已验证（切换后仅 1 路播放） |
| 4 | 音频循环接缝爆音 | 场景用长音频，接缝间隙不可闻；混音音源是 47 秒短循环，**素材层**已用等功率 wrap 交叉淡化做无缝；引擎层双源仍未实现 | ⚠️ 见记录 27 与「遗留优化」1 |
| 5 | 提示音与环境音抢音量 | 响铃时对 master 做 duck（×0.28），2.2–3.2 秒后恢复 | ✅ 已验证（`masterGain: 0.196`） |
| 6 | 切换场景声音硬切爆音 | `linearRampToValueAtTime` 1.2 秒交叉淡化 | ✅ 已验证（旧层 gain→0 后 pause） |
| 7 | 新增设置项会让老用户的 localStorage 缺字段 | `usePersistentState` 对对象类型与默认值浅合并 | ✅ 已验证（新增 9 个设置项后老数据仍可用） |
| 8 | 统计日志在 localStorage 里无限增长 | 日志上限 500 条、归档 180 天，超出丢弃最旧 | ✅ 已实现 |
| 9 | 外部域名失效导致视觉崩塌 | 字体与浮层图镜像到本地，视频用渐变兜底 | ✅ 已验证（外链字体数 0、离线可打开） |
| 10 | 计时状态高频更新导致整树重渲染 | 秒级状态更新 + 组件 memo | ✅ 已验证（5 秒内 5 次更新） |
| 11 | 刷新后内存态归零 | 会话（含分心次数）持久化 | ✅ 已验证（刷新后计数与剩余时间都延续） |
| 12 | 后台页面的视频持续耗电 | `visibilitychange` 时暂停视频，仅保留音频 | ✅ 已实现 |
| 13 | 低电量 / 省流环境下加载几十 MB 视频 | 自动切静态渐变，可在设置里手动强制 | ✅ 已实现 |
| 14 | 绝对定位元素依赖静态位置导致错位 | 一律显式写 `left` / `top`（记录 18） | ✅ 已修复并量过几何 |
| 15 | 任何手势都触发发声，用户还没开始就听到声音 | 解锁与发声分离，`setPlaying` 只由计时状态驱动（记录 19） | ✅ 已验证（待机 `context === null`） |
| 16 | 静音只改增益、媒体元素仍在解码耗电 | 淡出后真正 `pause()`，恢复时再 `play()`（记录 19） | ✅ 已验证（暂停后 4 层全部 pause） |
| 17 | 睡眠淡出与主音量共用一个 gain 互相打断 | 睡眠走独立 `sleepGain` 节点（记录 19） | ✅ 已验证（`master=0.7` 且 `sleepGain` 缓降） |
| 18 | 持续音调会被大脑当作旋律，长时间听很累 | 氛围层只用**无音高**的宽带噪声（记录 20） | ✅ 已换成 CC0 room tone |
| 19 | 低频压得太低，小喇叭放不出来反而变成嗡鸣 | 垫层 <150Hz 占比 52.5%、>1kHz 保留 8.3%（原 81.3% / 0%） | ✅ 频谱图与指标均已核 |
| 20 | 音层晚于设置调用创建，默认倍率 1 导致起点冒音 | 引擎用 `adaptiveTargets` 记住目标倍率（记录 20） | ✅ 已验证（起点增益 0，修前 0.527） |
| 21 | 提示层的层级低于抽屉遮罩，按钮点不到 | 提示层 z-index 提到遮罩之上（记录 21） | ✅ 已验证 |
| 22 | 集合合并产生重复项 → 重复渲染 + key 冲突 | 先去重再截断（记录 22） | ✅ 已验证（视频数量与 srcs 无重复） |
| 23 | 去抖写入把"已清空的数据"写回 | `clearAllData` 前 `suspendPersistence()`（记录 23） | ✅ 已验证 |
| 24 | 运行中误点阶段标签会静默丢掉进度 | 非 idle 时拦截并提示（交接文档 §0） | ✅ 浏览器已验证 |
| 25 | 损坏备份把结构错误的数据写进 localStorage | 逐字段 sanitize + 越界回落（交接文档 §0） | ✅ 单测覆盖 |
| 26 | CI 运行时版本低于依赖 `engines`，本地全绿而 CI 全红 | CI 与本地统一 Node 24；升级依赖后反查 `engines`（记录 24） | ✅ 已验证（CI 26 秒绿） |
| 27 | `github.com:443` 被阻断导致 `git push` 超时 | 推送走 SSH-over-443 一次性远端地址（记录 25） | ✅ 已验证（推送成功） |
| 28 | 自定义混音关着时仍预热它的 6 个文件 | `setRegistry` 只在 `mixerEnabled` 时纳入，省流模式下不白下载 3MB | ✅ 已实现 |
| 29 | 预设里写错音源 id —— 不报错，只是那个音源永远沉默 | 单测断言预设只引用 `MIXER_SOURCE_IDS` 中存在的 id | ✅ 单测覆盖 |
| 30 | 新增设置项漏改备份导入的白名单 / 嵌套对象缺键 | 默认值 / 预设 / 导入三入口统一过 `normalizeLevels()`；读取侧按 id 兜底（记录 28） | ✅ 单测覆盖 |
| 31 | 交叉淡化用默认线性曲线，交叉区掉 3dB | 一律用等功率曲线 `curve=qsin`（记录 27） | ✅ 已量过接缝指标 |
| 32 | 拖拽落点差一位（往下拖像没反应） | 抽成 `resolveMoveTarget` 纯函数 + 三种情况单测（记录 29） | ✅ 单测 + 浏览器实测 |
| 33 | 触屏上拖拽手柄被浏览器当成滚动 | 只手柄加 `touch-action: none`，整行不加（记录 30） | ⚠️ 待真机确认 |
| 34 | 键盘用户到不了拖拽交互 | 手柄写成 `<button>`，上下方向键移动一格 | ✅ 浏览器实测通过 |
| 35 | 排序打乱"未完成在前、已完成在后"的分组 | 只在同分组内移动，跨组只能靠勾选 / 取消勾选 | ✅ 单测覆盖 |
| 36 | 编辑已完成的任务后，回顾里还是旧标题 | 归档快照同步更新（`upsertArchiveTasks`），且只改它**实际归档的那一天** | ✅ 单测覆盖 |
| 37 | `clamp` 会把 `NaN` 原样透传，落进数据成为"看起来正常"的坏值（如 `0/NaN 番茄`）；落到 `splice` 上则变成"悄悄挪到最前" | 写入口统一归一化（`normalizeEstimate`）：非法值按"未提供"处理，**不夹成默认值** | ✅ 单测覆盖 |
| 38 | 拆分文件时误删了仍在使用的**类型** import —— ESLint 不报，172 个测试也全绿 | 拆完必须跑 `npm run build`（含 `tsc -b`）；lint 与单测都覆盖不到类型层的引用 | ✅ 本次被 tsc 拦下 |
| 39 | 新增设置项后，消费它的 React effect **依赖数组漏字段** —— 开关拨了但状态机没收到通知 | 设置项变更要同步四处：`types` / `defaults` / `backup.sanitize` / **消费它的 effect 依赖**。前三处类型与单测能兜住，第四处只能靠浏览器实测 | ✅ 本次实测抓到（`flowtimeMode`） |

---

## 遗留优化（尚未做，需要时再动）

1. **双源交叉淡化循环**：混音器的 6 个音源是 47 秒短循环，**素材层已用等功率 wrap 交叉淡化**
   消除了首尾接缝（记录 27），所以不会有爆音。剩下的问题是"听得出在循环"——
   那是素材长度决定的，要解决得在 `engine.ts` 里为每个 layer 增加 B 源，
   在 `duration - 1.5s` 处启动并做等功率交叉淡化。
2. **页面隐藏时暂停环境音**：省电，但会打断"背景播放"的预期，需产品决策。
3. **音频响度归一化**：4 个场景音源来自不同录音，主观响度不完全一致，
   可在 `scenes.ts` 的 `gain` 上继续微调（当前 0.9 / 1 / 0.9 / 0.9）。**需要人耳判断。**
   混音器的 6 个音源已统一做过 `loudnorm=I=-20:TP=-3`，**不要再按 RMS 反补** ——
   它们的 RMS 差异（0.014–0.111）反映的是素材疏密（鸟鸣大部分时间安静），不是响度不一致。
4. **统计数据的跨天刷新**：`computeStats` 以"渲染时的当前时间"为准，
   如果页面跨零点一直开着不刷新，今日数据不会自动滚到新的一天。影响极小（重渲染即修正）。
